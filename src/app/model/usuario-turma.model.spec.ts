import { UsuarioTurma } from './usuario-turma.model';

describe('UsuarioTurma', () => {
  it('should be defined', () => {
    const model: UsuarioTurma = {} as UsuarioTurma;
    expect(model).toBeDefined();
  });
});