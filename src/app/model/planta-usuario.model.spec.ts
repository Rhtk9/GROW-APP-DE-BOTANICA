import { PlantaUsuario } from './planta-usuario.model';

describe('PlantaUsuario', () => {
  it('should be defined', () => {
    const model: PlantaUsuario = {} as PlantaUsuario;
    expect(model).toBeDefined();
  });
});